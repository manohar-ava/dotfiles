#!/bin/bash

# Kanagawa Wave palette
# Active:   crystalBlue — visible accent
# Inactive: sumiInk3 — subtle, blends with bar

options=(
    style=round
    width=6.0
    hidpi=off
    active_color=0xFF7E9CD8
    inactive_color=0xFF363646
)

borders "${options[@]}"
